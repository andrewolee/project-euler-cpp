#ifndef NUMTHEORY_H
#define NUMTHEORY_H

#include <vector>
#include <unordered_set>

int gcd(int a, int b);

int pow(int a, int b);

int mod_pow(int a, int b, int m);

std::vector<int> eratosthenes(int n);

#endif